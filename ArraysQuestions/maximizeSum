class Solution {
    public int maximizeSum(int[] nums, int k) {
        int sum = 0;
        int max = Integer.MIN_VALUE;

        for(int i : nums) max = Math.max(max, i);

        while(k != 0) {
            sum += (max++);
            k--;
        }

        return sum;
    }
}
