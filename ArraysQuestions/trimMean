class Solution {
    public double trimMean(int[] arr) {
        Arrays.sort(arr);

        int n = arr.length;
        int five = n/20;
        double sum = 0;

        for(int i = five; i<n-five; i++) {
            sum += arr[i];
        }

        return (double)sum/(n - 2*five);
    }
}
