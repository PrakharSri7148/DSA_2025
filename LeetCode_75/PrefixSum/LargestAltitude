class Solution {
    public int largestAltitude(int[] gain) {
        
        int max_altitude = 0;
        int sum = 0;


        for(int i=0; i<gain.length; i++) {
            sum = sum + gain[i];
            if(sum >= max_altitude) max_altitude = sum;
        }

        return max_altitude;

    }
}
