class Solution {
    public int differenceOfSums(int n, int m) {
        
        int sumd = 0;
        int sumnd = 0;

        for(int i =1; i<= n; i++) {
            if(i % m == 0) sumd += i;
            else sumnd += i;
        }

        return sumnd - sumd;
    }
}
