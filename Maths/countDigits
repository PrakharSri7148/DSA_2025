class Solution {
    public int countDigits(int num) {
        
        int count = 0;
        int copy_num = num;

        while( num != 0) {
            int digit = num%10;

            if(copy_num % digit == 0) count ++;

            num /= 10;
        }

        return count;
    }
}
